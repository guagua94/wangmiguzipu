# 汪咪的谷子铺 — 团员端「时间轴 + 批量工作台」改造方案 v2.0

> 2026-08-23 | 基于方案五（时间轴信息架构）+ 方案四（谷圈原生视觉风格）融合，整合11项团员视角修正
> 文件位置：F:/谷子APP - 副本/wangmi/谷子时间轴改造方案_v2.md

---

## 一、核心原则

1. **首页回答"我接下来要做什么"** — Timeline 按时间驱动，纯提醒，不放操作按钮
2. **工作台回答"这件事怎么批量处理"** — 自动勾选 + 自适应选中数量，不弹确认浮窗
3. **子页面回答"这个东西现在什么情况"** — 统一卡片规范，支持左图右文扫描
4. **视觉风格：谷圈原生现代风** — 保留品牌粉 + 语义状态色，卡片白底分隔线，胶囊按钮

---

## 二、首页 Timeline 架构

### 2.1 页面结构（含修正#11：常驻进行中拼团栏）

```
┌────────────────────────────────────────────┐
│  汪咪的谷子时间轴                            │ ← 页面标题 22px/700/左对齐
│  2026-08-23 周日                             │ ← 日期辅助 13px/#8E8E93
├────────────────────────────────────────────┤
│                                              │
│  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐       │ ← 顶部数据卡片（横向滚动）
│  │  5  │  │  2  │  │  1  │  │  3  │       │
│  │待清货│  │待付款│  │转单待│  │进行中│       │
│  └─────┘  └─────┘  │确认  │  │拼团  │       │
│                     └─────┘  └─────┘       │
│                                              │
│  ┌─────────────────────────────────────────┐ │ ← 修正#11：进行中拼团横向滚动栏
│  │ 🟠 夏日祭系列 已排3件 截团：3天后        │ │
│  │ 🟠 海滨系列    已排1件 截团待定         │ │
│  │ 🔵 樱花限定    已排2件 截团：7天后       │ │
│  └─────────────────────────────────────────┘ │
│                                              │
│  ──── 接下来 ────                            │ ← 时间分组标签 13px/500/#8E8E93
│                                              │
│  今天 · 17:00                                │ ← 时间节点 12px/400/#C4C4C6
│  ┌───────────────────────────────────────┐  │ ← 节点卡片
│  │ 🟠 夏日祭系列拼团                       │  │
│  │    枫叶吧唧等3件 · 截团待付款              │  │ ← 修正#3：聚合显示
│  │    已排45/50 · 差5个成配 ⚠               │  │ ← 修正#9：成配进度
│  │              去付款 →                    │  │ ← "去付款" 文字链 #FF5B7F
│  └───────────────────────────────────────┘  │
│                                              │
│  今天 · 17:00                                │
│  ┌───────────────────────────────────────┐  │
│  │ 🔴 测试拍卖                             │  │
│  │    ⚠ 强制清货截止                       │  │
│  │    仓费已产生 ¥12.50                    │  │
│  │              去清货 →                  │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  今天 · 18:00                                │ ← 修正#8：同时间按紧急度排序
│  ┌───────────────────────────────────────┐  │
│  │ 🟣 星空拍卖                             │  │
│  │    45分钟后截拍 · 当前出价 ¥128          │  │ ← 修正#5：竞拍中状态
│  │              去竞拍 →                  │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  截团待定 · 夏日祭系列                       │ ← 修正#2：手动截团分组
│  ┌───────────────────────────────────────┐  │
│  │ 🟣 夏日祭系列拼团                       │  │
│  │    已排3件 · 截团时间待定               │  │
│  │              查看排表 →                 │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  3天后 · 8月26日                            │
│  ┌───────────────────────────────────────┐  │
│  │ 🟢 清货单 #128                          │  │ ← 修正#5：已完成清货
│  │    已发货 · 7天内自动确认               │  │
│  │              查看物流 →                 │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ──── 已完成 ────                           │ ← 默认折叠
│  （已完成 · 8 单）                          │
│  ▶                                           │
│                                              │
├────────────────────────────────────────────┤
│  [🏠] [🛒] [🎲] [📋] [👤]                  │ ← 底部导航栏
└────────────────────────────────────────────┘
```

### 2.2 进行中拼团常驻栏（修正#11）

**功能**：顶部固定展示所有"已参与但未截团"的拼团，让用户打开首页就知道自己排了哪些谷、截团状态如何。

**数据结构**：

```typescript
interface ActiveGroupCard {
  billId: number;
  seriesName: string;
  totalItems: number;       // 已排总件数
  deadlineAt: number | null; // null = 手动截团
  state: '进行中' | '截团待定';
  statusColor: string;       // 有deadline用蓝色，无deadline用紫色
}
```

**视觉规范**：

```css
.active-group-bar {
  display: flex;
  gap: 8px;
  overflow-x: auto;
  padding: 12px 16px;
  scrollbar-width: none;  /* 隐藏滚动条 */
}

.active-group-bar::-webkit-scrollbar {
  display: none;
}

.active-group-card {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 10px 14px;
  min-width: 160px;
  flex-shrink: 0;
  border-left: 4px solid var(--status-color);
}

.active-group-card .title {
  font-size: 14px;
  font-weight: 600;
  color: #1C1C1E;
}

.active-group-card .meta {
  font-size: 12px;
  color: #8E8E93;
  margin-top: 4px;
}

.active-group-card .deadline {
  font-size: 12px;
  font-weight: 500;
  color: var(--status-color);
  margin-top: 2px;
}
```

**点击行为**：点击后直接跳拼团详情/肾表页。

**与 Timeline 的关系**：
- 进行中拼团常驻栏显示所有参与中的拼团（无论是否有 deadline）
- Timeline 上只显示有明确 deadline 的拼团节点，按时间排序
- 无 deadline 的拼团在 Timeline 上放「截团待定」分组
- 两者互补：常驻栏快速概览，Timeline 按时间轴聚焦即将到期的事件

### 2.3 节点卡片规范（含修正#3/#4/#9）

**单节点数据结构**：

```typescript
interface TimelineNode {
  id: string;              // 全局唯一标识
  type: 'group' | 'sale' | 'auction' | 'transfer' | 'clearing' | 'aftersale';
  icon: string;            // emoji 状态色圆
  title: string;           // 主标题
  subtitle: string;        // 副标题（含聚合信息"等 N 件"）
  detailLine?: string;     // 修正#9：第三行，展示成配进度/附加信息
  deadlineAt: number | null; // null = 手动截团/无明确时间
  statusColor: string;     // 状态色值
  nextEventType: string;   // 事件类型文本
  urgencyLevel: number;    // 修正#8：紧急度权重 0-3
  linkAction: string;      // 跳转动作
  autoSelectId?: number;   // 跳转工作台时自动勾选
}
```

**节点卡片视觉规范**：

```css
.timeline-node-card {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 12px 16px;
  margin-bottom: 8px;
  border-bottom: 1px solid #F0F0F2;
  position: relative;
  overflow: hidden;
}

/* 修正#8：紧急度3（超期）左侧色条加粗到6px */
.timeline-node-card.urgency-3::before {
  width: 6px;
  background: #FF3B30;
}

/* 修正#8：紧急度2（待付款）左侧色条标准4px */
.timeline-node-card::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 4px;
  background: var(--status-color);
  border-radius: 12px 0 0 12px;
}

/* 修正#8：超期节点轻微泛红背景 */
.timeline-node-card.urgency-3 {
  background: linear-gradient(to right, #FFF5F5 0%, #FFFFFF 100%);
}

.timeline-node-title {
  font-size: 16px;
  font-weight: 600;
  color: #1C1C1E;
  padding-left: 12px;
}

.timeline-node-subtitle {
  font-size: 14px;
  font-weight: 400;
  color: #8E8E93;
  padding-left: 12px;
  margin-top: 4px;
}

/* 修正#9：第三行详细信息 */
.timeline-node-detail {
  font-size: 13px;
  font-weight: 400;
  padding-left: 12px;
  margin-top: 4px;
}

.timeline-node-detail.warning {
  color: #FF3B30;  /* 砍单/不足预警 */
}

.timeline-node-action {
  font-size: 14px;
  font-weight: 500;
  color: #FF5B7F;
  padding-left: 12px;
  margin-top: 8px;
  text-align: right;
}
```

**卡片点击态**：

```css
.timeline-node-card:active {
  background: #FFF5F7;
  transform: scale(0.98);
  transition: all 0.1s ease;
}
```

### 2.4 顶部数据卡片

```css
.stat-card {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 12px 16px;
  min-width: 80px;
  text-align: center;
  border: 1px solid #F0F0F2;
}

.stat-card .number {
  font-size: 32px;
  font-weight: 700;
  color: #FF5B7F;
  line-height: 1;
}

.stat-card .label {
  font-size: 12px;
  font-weight: 400;
  color: #8E8E93;
  margin-top: 4px;
}
```

**点击行为**：
- 点击后 Timeline 自动过滤为只显示对应类型的节点
- 过滤状态高亮该卡片，其他卡片置灰
- 再次点击同一卡片取消过滤，恢复全部
- **修正#11**：新增"进行中拼团"卡片，点击过滤只显示进行中拼团节点

### 2.5 时间分组（含修正#2/#6）

**修正#6：Timeline 不做秒级倒计时，只显示静态时间节点**：

| 分组标签 | 时间范围 | 显示格式 | 样式 |
|:---|:---|:---|:---|
| 今天 | deadlineAt 在 24h 内 | "今天 · 17:00" | 正常显示 |
| 明天 | 24h-48h | "明天 · 8月24日" | 正常显示 |
| X天后 | 2-7 天 | "3天后 · 8月26日" | 正常显示 |
| 本周 | 7-14 天 | "下周三 · 8月30日" | 灰色折叠 |
| 稍后 | >14天 或 无 deadline | "稍后" | 灰色折叠 |
| 截团待定 | deadlineAt = null（手动截团） | "截团待定 · 系列名" | 紫色标识 |
| 已完成 | 状态为已完成 | "已完成 · X 单" | 默认折叠 |

**修正#6：即将截止标签**：

```css
.deadline-soon-badge {
  display: inline-block;
  padding: 2px 6px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 500;
  background: #FFF5E6;
  color: #FF9500;
  margin-left: 4px;
}

/* 超期用红色 */
.deadline-over-badge {
  background: #FFEBEE;
  color: #FF3B30;
}
```

当 deadline 在 24h 内时，在节点副标题右侧显示「⚠ 即将截止」标签；已超期显示「🔴 已超期」。

### 2.6 底部过滤栏

```
[全部] [待我操作](默认选中) [进行中] [已完成 ▼]
```

**过滤逻辑**：
- **全部**：显示所有节点 + 常驻进行中拼团栏
- **待我操作**：只显示需要用户主动操作的节点（同 v1.0）
- **进行中**：显示状态为"进行中/囤货中/已排"的节点 + 常驻进行中拼团栏
- **已完成**：展开显示已完成节点

---

## 三、节点详情浮层（修正#7）

### 3.1 交互流程

**修正#7：节点点击不直接跳转操作页，先弹出详情浮层**：

```
1. 团员点击 Timeline 节点
2. 底部弹出详情浮层（半屏/70%高度）
3. 浮层展示完整信息：商品图、价格、时间线、操作说明
4. 浮层底部有粉色胶囊按钮「去XX」
5. 团员点击「去XX」才真正跳转对应操作页/工作台
6. 团员也可以直接下滑关闭浮层，不操作
```

### 3.2 浮层结构

```
┌────────────────────────────────────────────┐
│  ━━━━━━ 拖动关闭                            │ ← 顶部拖动条
├────────────────────────────────────────────┤
│  ┌────┐                                    │
│  │ 🟠 │  夏日祭系列拼团                      │ ← 大图标 + 标题
│  └────┘  枫叶吧唧等3件 · 截团待付款           │ ← 副标题
│                                              │
│  ─── 详细信息 ───                           │
│  截团时间：2026-08-23 17:00                 │
│  已排谷子：枫叶吧唧#3、海盐吧唧#7            │
│           花火立牌#12                       │
│  总金额：¥128.00                            │
│  成配状态：45/50 · 差5个成配 ⚠              │ ← 修正#9
│                                              │
│  ─── 时间线 ───                             │
│  ● 已排单（8/20 14:30）                     │
│  ● 截团（8/23 17:00） ← 当前                │
│  ○ 付款截止（8/25 17:00）                   │
│  ○ 入囤                                    │
│  ○ 清货                                    │
│                                              │
│  ┌───────────────────────────────────────┐│
│  │        去付款（¥128.00）                ││ ← 粉色胶囊主按钮
│  └───────────────────────────────────────┘│
│                                              │
│  取消                                       │ ← 次按钮
└────────────────────────────────────────────┘
```

### 3.3 浮层样式

```css
.detail-sheet {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: #FFFFFF;
  border-radius: 20px 20px 0 0;
  max-height: 75vh;
  overflow-y: auto;
  z-index: 200;
  animation: slideUp 0.3s ease-out;
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

.detail-sheet .drag-handle {
  width: 36px;
  height: 4px;
  background: #E5E5EA;
  border-radius: 2px;
  margin: 8px auto;
}

.detail-sheet .content {
  padding: 16px;
}

.detail-sheet .timeline-mini {
  padding-left: 16px;
  border-left: 2px solid #F0F0F2;
}

.detail-sheet .timeline-mini .event {
  position: relative;
  padding: 8px 0 8px 12px;
  font-size: 14px;
  color: #8E8E93;
}

.detail-sheet .timeline-mini .event.current {
  color: #FF5B7F;
  font-weight: 500;
}

.detail-sheet .timeline-mini .event.current::before {
  content: '';
  position: absolute;
  left: -19px;
  top: 12px;
  width: 8px;
  height: 8px;
  background: #FF5B7F;
  border-radius: 50%;
}
```

---

## 四、批量清货工作台（含修正#10）

### 4.1 入口

1. 点击 Timeline 节点浮层上的 **「去清货」** 按钮
2. 点击顶部 **「待清货 X 单」** 数据卡片

**场景**：
- 从 Timeline 节点进入：自动勾选该单
- 从顶部卡片进入：无自动勾选

### 4.2 页面结构（含修正#10：分组标题全选）

```
┌────────────────────────────────────────────┐
│  ← 返回  📦 批量清货                         │
├────────────────────────────────────────────┤
│                                              │
│  ☑️ ─── 超期（请尽快处理） ───             │ ← 修正#10：分组标题带全选checkbox
│                                              │
│  ┌───────────────────────────────────────┐  │
│  │ ☑️ 🔴 测试拍卖                         │  │
│  │    入囤45天 · 超期15天                  │  │
│  │    仓费 ¥12.50                         │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ☑️ ─── 7天内到期 ───                      │ ← 修正#10：分组标题全选
│                                              │
│  ┌───────────────────────────────────────┐  │
│  │ ☐ 🟠 夏日祭系列拼团                     │  │
│  │    免费剩余3天                         │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ─── 正常囤货（12单）───                    │ ← 修正#10：默认折叠
│  ▶                                           │
│                                              │
│  ─────────────────────────────────────────  │
│  已选 2 单              预估 ¥36.50         │
│  [选择邮费 ▼] [选择打包费 ▼]                │
│  [请选择地址 ▼]                             │
│  ┌───────────────────────────────────────┐  │
│  │        发起清货（2单 · ¥36.50）          │  │
│  └───────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

### 4.3 修正#10：分组全选与折叠规范

**分组标题组件**：

```css
.stock-group-header {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 0 8px 0;
  font-size: 13px;
  font-weight: 500;
}

.stock-group-header .checkbox {
  width: 18px;
  height: 18px;
  border-radius: 4px;
  border: 2px solid #E5E5EA;
  cursor: pointer;
}

.stock-group-header .checkbox.checked {
  background: #FF5B7F;
  border-color: #FF5B7F;
}

/* 超期组 */
.stock-group-header.overdue {
  color: #FF3B30;
}

/* 7天内组 */
.stock-group-header.soon {
  color: #FF9500;
}

/* 正常组 */
.stock-group-header.normal {
  color: #8E8E93;
}
```

**分组交互规则**：
1. **超期组**：默认展开，标题 checkbox 默认全选，可取消
2. **7天内到期**：默认展开，标题 checkbox 默认不选，可全选
3. **正常囤货**：默认折叠（减少视觉噪音），点击展开后 checkbox 默认不选
4. **无全局全选按钮**：避免误触跨组全选

**全选逻辑**：
- 点击分组标题 checkbox：全选/取消选该分组内所有未禁用的囤货单
- 组内某单手动取消勾选：该分组标题 checkbox 变为半选状态（indeterminate）
- 组内全部手动勾选：该分组标题 checkbox 自动变为选中状态

### 4.4 囤货卡片

```css
.stock-card {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 12px 16px;
  margin-bottom: 8px;
  border-bottom: 1px solid #F0F0F2;
  display: flex;
  align-items: flex-start;
  gap: 12px;
}

.stock-card .checkbox {
  width: 20px;
  height: 20px;
  border-radius: 4px;
  border: 2px solid #E5E5EA;
  flex-shrink: 0;
  margin-top: 2px;
  cursor: pointer;
  transition: all 0.15s;
}

.stock-card .checkbox.checked {
  background: #FF5B7F;
  border-color: #FF5B7F;
}

.stock-card .content {
  flex: 1;
  min-width: 0;
}

.stock-card .title {
  font-size: 16px;
  font-weight: 600;
  color: #1C1C1E;
}

.stock-card .subtitle {
  font-size: 14px;
  color: #8E8E93;
  margin-top: 4px;
}

.stock-card .fee {
  font-size: 14px;
  font-weight: 600;
  color: #FF3B30;
  margin-top: 4px;
}
```

---

## 五、子页面统一卡片规范

### 5.1 卡片基础结构

```
┌────────────────────────────────────────────┐
│  ┌────┐  标题行（16px/600/#1C1C1E）         │
│  │ 🟠 │  副标题行（14px/400/#8E8E93）       │
│  └────┘  操作行（14px/500/#FF5B7F 右对齐）  │
└────────────────────────────────────────────┘
```

### 5.2 状态色规范

| 状态 | 颜色 | 用途 |
|:---|:---|:---|
| 待付款/待操作 | `#FF9500` 橙色 | 需要用户主动行动 |
| 进行中/囤货中/已排 | `#007AFF` 蓝色 | 正常进行 |
| 已完成/成功 | `#34C759` 绿色 | 流程结束 |
| 超期/警告/失败 | `#FF3B30` 红色 | 需要紧急处理 |
| 截团/截止 | `#5856D6` 紫色 | 时间类事件 |

### 5.3 标签(Tag)规范

```css
.tag {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 500;
  line-height: 1.4;
}

.tag-pink { background: #FFE4EA; color: #FF5B7F; }
.tag-blue { background: #E8F4FF; color: #007AFF; }
.tag-green { background: #E8F8ED; color: #34C759; }
.tag-orange { background: #FFF5E6; color: #FF9500; }
.tag-red { background: #FFEBEE; color: #FF3B30; }
.tag-purple { background: #F0E8FF; color: #5856D6; }
.tag-gray { background: #F2F2F7; color: #8E8E93; }
```

### 5.4 按钮规范

```css
/* 主按钮 */
.btn-primary {
  background: #FF5B7F;
  color: #FFFFFF;
  border: none;
  border-radius: 999px;
  padding: 12px 24px;
  font-size: 15px;
  font-weight: 600;
  min-height: 48px;
  width: 100%;
  cursor: pointer;
  transition: all 0.15s;
}

.btn-primary:hover { background: #E0486B; }
.btn-primary:active { transform: scale(0.97); }
.btn-primary:disabled { opacity: 0.4; cursor: not-allowed; }

/* 次按钮 */
.btn-secondary {
  background: #FFFFFF;
  color: #FF5B7F;
  border: 1.5px solid #FF5B7F;
  border-radius: 8px;
  padding: 10px 20px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.15s;
}

/* 文字链 */
.link-action {
  color: #FF5B7F;
  font-size: 14px;
  font-weight: 500;
  text-decoration: none;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 4px;
}

.link-action::after {
  content: '→';
  font-size: 16px;
}
```

---

## 六、拼团 Tab 改造（修正#1）

### 6.1 默认页面：我的拼团

**修正#1：底部导航「拼团」tab 默认展示「我的拼团」列表，而非大厅**：

```
┌────────────────────────────────────────────┐
│  我的拼团                     [去大厅 →]   │ ← 顶部标题 + 去大厅按钮
├────────────────────────────────────────────┤
│                                              │
│  ┌───────────────────────────────────────┐  │
│  │ 🟠 夏日祭系列拼团                       │  │
│  │    已排3件 · 截团：3天后                │  │
│  │    枫叶吧唧 45/50 · 差5个成配 ⚠        │  │ ← 修正#9：成配进度
│  │              查看肾表 →                │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ┌───────────────────────────────────────┐  │
│  │ 🟣 海滨系列拼团                       │  │
│  │    已排1件 · 截团时间待定               │  │ ← 修正#2：手动截团
│  │              查看肾表 →                │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ┌───────────────────────────────────────┐  │
│  │ 🔵 樱花限定拼团                       │  │
│  │    已排2件 · 截团：7天后                │  │
│  │              查看肾表 →                │  │
│  └───────────────────────────────────────┘  │
│                                              │
└────────────────────────────────────────────┘
```

**「去大厅」按钮**：

```css
.go-hall-btn {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 6px 14px;
  background: #FF5B7F;
  color: #FFFFFF;
  border-radius: 999px;
  font-size: 13px;
  font-weight: 500;
  text-decoration: none;
}
```

### 6.2 大厅页面

点击「去大厅」后进入现有拼团大厅，不做改造。

---

## 七、拍卖节点全面覆盖（修正#5）

### 7.1 拍卖生命周期 Timeline 节点

| 状态 | 优先级 | 颜色 | 标题 | 副标题 | 事件类型 | 位置 |
|:---|:---|:---|:---|:---|:---|:---|
| **竞拍中** | P2 | 🟣 紫色 | 拍卖品名 | "45分钟后截拍 · 当前出价 ¥128" | 截拍提醒 | 按截拍时间正常排序 |
| **中标待付款** | P0 | 🟠 橙色 | 拍卖品名 | "中标！24h内付款 ¥128" | 付款截止 | **置顶最高优先级** |
| **囤货中** | P1 | 🔵 蓝色 | 拍卖品名 | "入囤12天 · 免费剩余18天" | 正常囤货 | 按清货deadline排序 |
| **流拍退款** | P3 | 🟢 绿色 | 拍卖品名 | "已流拍 · 保证金¥50已退余额" | 退款完成 | 已完成分组 |
| **超期清货** | P0 | 🔴 红色 | 拍卖品名 | "入囤45天 · 超期15天 · 仓费¥12.50" | 超期清货 | 置顶 |

### 7.2 中标待付款节点（最高优先级）

```typescript
{
  id: `auction-${auction.id}-pay`,
  type: 'auction',
  icon: '🟠',
  title: auction.name,
  subtitle: `中标！${getRemainingHours(auction.wonAt, 24)}小时内付款 ¥${auction.curPrice}`,
  deadlineAt: auction.wonAt + 24 * 3600 * 1000,
  statusColor: '#FF9500',
  nextEventType: '中标待付款',
  urgencyLevel: 2,  // 高紧急
  linkAction: 'auction',
  autoSelectId: auction.id,
}
```

### 7.3 竞拍中节点

```typescript
{
  id: `auction-${auction.id}-bidding`,
  type: 'auction',
  icon: '🟣',
  title: auction.name,
  subtitle: `${getRemainingTime(auction.endAt)}后截拍 · 当前出价 ¥${auction.curPrice}`,
  deadlineAt: auction.endAt,
  statusColor: '#5856D6',
  nextEventType: '截拍提醒',
  urgencyLevel: 1,  // 中紧急（关注但不一定要操作）
  linkAction: 'auction',
}
```

### 7.4 流拍退款节点

```typescript
{
  id: `auction-${auction.id}-refund`,
  type: 'auction',
  icon: '🟢',
  title: auction.name,
  subtitle: `已流拍 · 保证金¥${auction.depositAmount}已退余额`,
  deadlineAt: Date.now(),  // 已完成，放已完成分组
  statusColor: '#34C759',
  nextEventType: '退款完成',
  urgencyLevel: 0,
  linkAction: 'balance',  // 跳余额页面查看
}
```

---

## 八、排序规则（修正#8）

### 8.1 二级排序算法

**第一级：时间（deadlineAt 升序）**
**第二级：紧急度权重（同时间时）**

```typescript
// 紧急度定义
const URGENCY = {
  OVERDUE_CLEARING: 3,    // 超期清货/超期囤货（红色）
  PENDING_PAYMENT: 2,    // 待付款/中标待付款（橙色）
  DEADLINE_SOON: 1,      // 截团/截拍/即将到期（紫色）
  NORMAL: 0,             // 正常进行中（蓝色/绿色）
};

// 排序函数
nodes.sort((a, b) => {
  // 第一级：时间升序（null 放最后）
  const timeA = a.deadlineAt ?? Number.MAX_SAFE_INTEGER;
  const timeB = b.deadlineAt ?? Number.MAX_SAFE_INTEGER;
  if (timeA !== timeB) return timeA - timeB;
  
  // 第二级：紧急度降序（同时间时，紧急的在前）
  return b.urgencyLevel - a.urgencyLevel;
});
```

### 8.2 视觉强化

```css
/* 紧急度3：超期 — 左侧色条加粗 + 泛红背景 */
.timeline-node-card.urgency-3::before {
  width: 6px;
}
.timeline-node-card.urgency-3 {
  background: linear-gradient(to right, #FFF5F5 0%, #FFFFFF 100%);
}

/* 紧急度2：待付款 — 标准色条 */
.timeline-node-card.urgency-2::before {
  width: 4px;
}

/* 紧急度1：截团/截拍 — 细色条 */
.timeline-node-card.urgency-1::before {
  width: 3px;
  opacity: 0.6;
}
```

---

## 九、视觉设计规范

### 9.1 配色体系

```css
:root {
  /* 品牌色 */
  --brand: #FF5B7F;
  --brand-dark: #E0486B;
  --brand-bg: #FFE4EA;
  --brand-bg2: #FFF5F7;

  /* 语义状态色 */
  --status-pending: #FF9500;    /* 待付款/待操作 */
  --status-active: #007AFF;     /* 进行中/囤货中 */
  --status-done: #34C759;       /* 已完成 */
  --status-warning: #FF3B30;    /* 超期/警告 */
  --status-deadline: #5856D6;   /* 截团/截止 */

  /* 文字层级 */
  --t1: #1C1C1E;
  --t2: #3A3A3C;
  --t3: #8E8E93;
  --t4: #C4C4C6;

  /* 背景/边框 */
  --bg: #FAFAFA;
  --card: #FFFFFF;
  --line: #F0F0F2;
  --line2: #E5E5EA;
}
```

### 9.2 字号体系

| 层级 | 字号 | 字重 | 用途 | 色值 |
|:---|:---|:---|:---|:---|
| 页面大标题 | 22px | 700 | "汪咪的谷子时间轴" | --t1 |
| 数据卡片数字 | 32px | 700 | 仪表盘"5" | --brand |
| 卡片标题 | 16px | 600 | 系列名、商品名 | --t1 |
| 正文 | 14px | 400 | 常规内容 | --t2 |
| 辅助文字 | 12px | 400 | 日期、状态、金额说明 | --t3 |
| 时间分组标签 | 13px | 500 | "今天 · 17:00" | --t3 |
| 标签文字 | 11px | 500 | 拼团/直售/拍卖标签 | 白色 |

### 9.3 间距/圆角/阴影规范

同 v1.0，全局不使用阴影，用分隔线替代。

---

## 十、动效与交互规范

### 10.1 页面过渡

```css
.page-enter {
  animation: slideIn 0.25s ease-out;
}

@keyframes slideIn {
  from { opacity: 0; transform: translateX(30px); }
  to { opacity: 1; transform: translateX(0); }
}
```

### 10.2 详情浮层动画（修正#7）

```css
.detail-sheet {
  animation: slideUp 0.3s cubic-bezier(0.16, 1, 0.3, 1);
}

@keyframes slideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

/* 关闭动画 */
.detail-sheet.closing {
  animation: slideDown 0.25s ease-in forwards;
}

@keyframes slideDown {
  from { transform: translateY(0); }
  to { transform: translateY(100%); }
}
```

### 10.3 卡片点击态

```css
.card {
  transition: all 0.1s ease;
}

.card:active {
  background: #FFF5F7;
  transform: scale(0.98);
}
```

### 10.4 数字滚动动画

```javascript
function animateNumber(el, from, to, duration = 500) {
  const start = performance.now();
  function tick(now) {
    const progress = Math.min((now - start) / duration, 1);
    const value = Math.round(from + (to - from) * progress);
    el.textContent = value;
    if (progress < 1) requestAnimationFrame(tick);
  }
  requestAnimationFrame(tick);
}
```

### 10.5 下拉刷新

```css
.pull-refresh {
  position: relative;
  overflow: hidden;
}

.pull-refresh-indicator {
  position: absolute;
  top: -50px;
  left: 0;
  right: 0;
  height: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 0.2s;
}

.pull-refresh-indicator.pulling {
  transform: translateY(50px);
}
```

### 10.6 Toast 提示

```css
.toast {
  position: fixed;
  top: 16px;
  left: 50%;
  transform: translateX(-50%) translateY(-100px);
  background: #1C1C1E;
  color: #FFFFFF;
  padding: 10px 20px;
  border-radius: 8px;
  font-size: 14px;
  z-index: 1000;
  opacity: 0;
  transition: all 0.3s ease;
}

.toast.show {
  transform: translateX(-50%) translateY(0);
  opacity: 1;
}

.toast.success { background: #34C759; }
.toast.error { background: #FF3B30; }
```

---

## 十一、数据聚合方案

### 11.1 推荐：前端聚合（无需新增后端接口）

**理由**：
- `loadMe()` 已加载全部所需数据（myBills、myBuys、myAuctionOrders、myTransfers、myClears）
- 聚合逻辑主要做格式化，无复杂计算
- 避免增加后端复杂度，前后端解耦

**聚合函数伪代码**：

```typescript
function buildTimeline(
  myBills: GroupBill[],
  myBuys: Order[],
  myAuctionOrders: Auction[],
  myTransfers: Transfer[],
  myClears: Clearing[],
  shopCfg: ShopConfig
): TimelineData {
  const nodes: TimelineNode[] = [];
  const activeGroups: ActiveGroupCard[] = [];

  // 1. 进行中拼团 → 常驻栏 + Timeline 节点
  for (const bill of myBills) {
    const totalItems = bill.items.reduce((sum, i) => sum + i.qty, 0);
    
    // 常驻栏数据
    activeGroups.push({
      billId: bill.id,
      seriesName: bill.seriesName,
      totalItems,
      deadlineAt: bill.deadlineAt,
      state: bill.deadlineAt ? '进行中' : '截团待定',
      statusColor: bill.deadlineAt ? '#007AFF' : '#5856D6',
    });

    // 截团后的付款节点
    if (bill.state === '待付款') {
      // 修正#3：按系列聚合
      const itemNames = bill.items.map(i => `${i.name} #${i.seqs}`).join('、');
      const totalQty = bill.items.reduce((s, i) => s + i.qty, 0);
      const displayItems = totalQty > 1 
        ? `${bill.items[0].name}等${totalQty}件` 
        : itemNames;
      
      // 修正#9：计算成配进度
      const progressInfo = calculateGroupProgress(bill, shopCfg);
      
      nodes.push({
        id: `bill-${bill.id}`,
        type: 'group',
        icon: '🟠',
        title: bill.seriesName,
        subtitle: `${displayItems} · 截团待付款`,
        detailLine: progressInfo,  // "已排45/50 · 差5个成配 ⚠"
        deadlineAt: new Date(bill.createdAt).getTime() + 48 * 3600 * 1000,
        statusColor: '#FF9500',
        nextEventType: '截团待付款',
        urgencyLevel: 2,
        linkAction: 'orders',
      });
    }
    
    // 修正#2：手动截团且未截团 → 截团待定分组
    if (!bill.deadlineAt && bill.state === '进行中') {
      nodes.push({
        id: `bill-${bill.id}-pending`,
        type: 'group',
        icon: '🟣',
        title: bill.seriesName,
        subtitle: `已排${totalItems}件 · 截团时间待定`,
        deadlineAt: null,  // 无 deadline，不参与时间排序
        statusColor: '#5856D6',
        nextEventType: '截团待定',
        urgencyLevel: 0,
        linkAction: 'group',
      });
    }
  }

  // 2. 直售：待付款
  for (const buy of myBuys.filter(o => o.status === '待付款')) {
    const itemNames = buy.items.map(i => i.name).join('、');
    const totalQty = buy.items.reduce((s, i) => s + i.qty, 0);
    // 修正#4：多件显示"等 N 件"
    const displayTitle = totalQty > 1 
      ? `${buy.items[0].name}等${totalQty}件` 
      : itemNames;
    
    nodes.push({
      id: `order-${buy.id}`,
      type: 'sale',
      icon: '🟠',
      title: displayTitle,
      subtitle: `待付款 · ¥${buy.total}`,
      deadlineAt: new Date(buy.createdAt).getTime() + 24 * 3600 * 1000,
      statusColor: '#FF9500',
      nextEventType: '付款截止',
      urgencyLevel: 2,
      linkAction: 'orders',
      autoSelectId: buy.id,
    });
  }

  // 3. 拍卖：全面覆盖（修正#5）
  for (const auction of myAuctionOrders) {
    // 3.1 竞拍中
    if (auction.state === '拍卖中' && !auction.isWinner) {
      nodes.push({
        id: `auction-${auction.id}-bidding`,
        type: 'auction',
        icon: '🟣',
        title: auction.name,
        subtitle: `${formatRemaining(auction.endAt)}后截拍 · 当前出价 ¥${auction.curPrice}`,
        deadlineAt: auction.endAt,
        statusColor: '#5856D6',
        nextEventType: '截拍提醒',
        urgencyLevel: 1,
        linkAction: 'auction',
      });
    }
    
    // 3.2 中标待付款（最高优先级）
    if (auction.state === '待付款' && auction.isWinner) {
      nodes.push({
        id: `auction-${auction.id}-pay`,
        type: 'auction',
        icon: '🟠',
        title: auction.name,
        subtitle: `中标！${formatRemaining(auction.wonAt + 24 * 3600 * 1000)}内付款 ¥${auction.curPrice}`,
        deadlineAt: auction.wonAt + 24 * 3600 * 1000,
        statusColor: '#FF9500',
        nextEventType: '中标待付款',
        urgencyLevel: 2,
        linkAction: 'auction',
        autoSelectId: auction.id,
      });
    }
    
    // 3.3 囤货中/超期
    if (auction.state === '囤货中') {
      const days = getStockDays(auction);
      const overDays = Math.max(0, days - 7);  // 拍卖7天免费期
      const freeLeft = Math.max(0, 7 - days);
      
      let subtitle: string;
      let statusColor: string;
      let urgency: number;
      
      if (overDays > 0) {
        subtitle = `入囤${days}天 · 超期${overDays}天 · 仓费¥${calcOverFee(auction).toFixed(2)}`;
        statusColor = '#FF3B30';
        urgency = 3;
      } else if (freeLeft <= 3) {
        subtitle = `入囤${days}天 · 免费剩余${freeLeft}天 ⚠`;
        statusColor = '#FF9500';
        urgency = 1;
      } else {
        subtitle = `入囤${days}天 · 免费剩余${freeLeft}天`;
        statusColor = '#007AFF';
        urgency = 0;
      }
      
      nodes.push({
        id: `stock-auction-${auction.id}`,
        type: 'auction',
        icon: overDays > 0 ? '🔴' : (freeLeft <= 3 ? '🟠' : '🔵'),
        title: auction.name,
        subtitle,
        deadlineAt: auction.paidAt ? new Date(auction.paidAt).getTime() + 7 * 24 * 3600 * 1000 : Date.now(),
        statusColor,
        nextEventType: overDays > 0 ? '超期清货' : (freeLeft <= 3 ? '即将到期' : '正常囤货'),
        urgencyLevel: urgency,
        linkAction: 'stock',
        autoSelectId: auction.id,
      });
    }
    
    // 3.4 流拍退款
    if (auction.state === '流拍' && auction.depositRefunded) {
      nodes.push({
        id: `auction-${auction.id}-refund`,
        type: 'auction',
        icon: '🟢',
        title: auction.name,
        subtitle: `已流拍 · 保证金¥${auction.depositAmount}已退余额`,
        deadlineAt: Date.now(),
        statusColor: '#34C759',
        nextEventType: '退款完成',
        urgencyLevel: 0,
        linkAction: 'balance',
      });
    }
  }

  // 4. 转单
  for (const t of myTransfers.filter(t => 
    ['待接收者确认', '待管理员审核', '待接收者付款'].includes(t.state)
  )) {
    nodes.push({
      id: `transfer-${t.id}`,
      type: 'transfer',
      icon: '🟠',
      title: t.name,
      subtitle: `${t.fromCn} → ${t.toCn} · ${t.state}`,
      deadlineAt: t.deadline,
      statusColor: '#FF9500',
      nextEventType: t.state,
      urgencyLevel: 2,
      linkAction: 'transfer',
    });
  }

  // 5. 清货单
  for (const c of myClears.filter(c => ['待付款', '已发货'].includes(c.state))) {
    nodes.push({
      id: `clearing-${c.id}`,
      type: 'clearing',
      icon: c.state === '待付款' ? '🟠' : '🔵',
      title: `清货单 #${c.id}`,
      subtitle: `${c.state} · ¥${c.total}`,
      deadlineAt: c.state === '待付款' 
        ? Date.now() + 24 * 3600 * 1000 
        : Date.now() + 7 * 24 * 3600 * 1000,
      statusColor: c.state === '待付款' ? '#FF9500' : '#007AFF',
      nextEventType: c.state,
      urgencyLevel: c.state === '待付款' ? 2 : 0,
      linkAction: 'stock',
    });
  }

  // 修正#8：二级排序
  nodes.sort((a, b) => {
    const timeA = a.deadlineAt ?? Number.MAX_SAFE_INTEGER;
    const timeB = b.deadlineAt ?? Number.MAX_SAFE_INTEGER;
    if (timeA !== timeB) return timeA - timeB;
    return b.urgencyLevel - a.urgencyLevel;
  });

  // 修正#2：把 deadlineAt = null 的节点移到「截团待定」虚拟分组
  const timeSortedNodes = nodes.filter(n => n.deadlineAt !== null);
  const pendingNodes = nodes.filter(n => n.deadlineAt === null);

  return {
    stats: {
      pendingClearing: myBuys.filter(o => isStockUrgent(o)).length + myAuctionOrders.filter(a => a.state === '囤货中' && isStockUrgent(a)).length,
      pendingPayment: myBills.filter(b => b.state === '待付款').length + myBuys.filter(o => o.status === '待付款').length + myAuctionOrders.filter(a => a.state === '待付款' && a.isWinner).length,
      pendingTransfer: myTransfers.filter(t => t.state === '待接收者确认').length,
      pendingReceive: myClears.filter(c => c.state === '已发货').length,
      activeGroups: activeGroups.length,  // 修正#11
    },
    activeGroups,  // 修正#11：常驻栏数据
    nodes: [...timeSortedNodes, ...pendingNodes],  // 截团待定放最后
  };
}
```

---

## 十二、底部导航栏改造

### 12.1 导航项

```
[🏠 首页] [🛒 拼团] [🎲 拍卖] [📋 订单] [👤 我的]
```

- **首页**：Timeline 仪表盘
- **拼团**：修正#1 — 默认「我的拼团」，顶部有「去大厅」按钮
- **拍卖**：拍卖大厅（保留现有）
- **订单**：订单列表（原"我的"里的订单模块独立出来）
- **我的**：个人中心

---

## 十三、改造范围与优先级

### 第一阶段（MVP）

| 优先级 | 任务 | 范围 | 工作量 |
|:---|:---|:---|:---|
| P0 | Timeline 首页 | 新建 `Timeline.vue`，含常驻进行中拼团栏 | 3天 |
| P0 | 顶部数据卡片 | 含"进行中拼团"卡片，点击过滤 | 0.5天 |
| P0 | 底部导航改造 | 新增"首页"tab，拼团tab默认我的拼团 | 0.5天 |
| P0 | 节点详情浮层 | 修正#7：半屏浮层，展示详情+时间线 | 1天 |
| P1 | 批量清货工作台 | 含修正#10：分组全选+默认折叠 | 1.5天 |
| P1 | 统一卡片样式 | Timeline节点、工作台、子页面统一 | 0.5天 |

### 第二阶段

| 优先级 | 任务 | 范围 |
|:---|:---|:---|
| P2 | 订单页改造 | Chip过滤 + 卡片规范化 |
| P2 | 转单页改造 | 卡片规范化 + 倒计时 |
| P2 | 肾表页改造 | 按系列折叠 |
| P2 | 余额/售后等 | 统一卡片 + 按钮规范 |

### 第三阶段

| 优先级 | 任务 | 范围 |
|:---|:---|:---|
| P3 | 动效 | 页面过渡、下拉刷新、数字滚动、Toast |

---

## 十四、关键决策确认清单

| # | 决策 | 状态 | 修正来源 |
|:---|:---|:---|:---|
| 1 | Timeline 首页纯提醒，节点不放操作按钮 | ✅ 已确认 | 原始方案 |
| 2 | 批量清货工作台是唯一操作入口 | ✅ 已确认 | 原始方案 |
| 3 | Timeline 节点"去清货"跳转工作台并自动勾选 | ✅ 已确认 | 原始方案 |
| 4 | 工作台自适应选中数量，不弹确认浮窗 | ✅ 已确认 | 原始方案 |
| 5 | 配色保留品牌粉，增加语义状态色 | ✅ 已确认 | 原始方案 |
| 6 | 卡片用左图右文 + 分隔线，不用阴影 | ✅ 已确认 | 原始方案 |
| 7 | 按钮用胶囊形主按钮 + 次按钮 + 文字链 | ✅ 已确认 | 原始方案 |
| 8 | 首页改为 Timeline，底部导航新增"首页"tab | ✅ 已确认 | 原始方案 |
| 9 | 工作台按紧急度分组排序 | ✅ 已确认 | 原始方案 |
| 10 | 前端聚合方案（无需新增后端接口） | ✅ 已确认 | 原始方案 |
| 11 | **拼团 tab 默认「我的拼团」+ 顶部「去大厅」** | ✅ 已确认 | **修正#1** |
| 12 | **手动截团放「截团待定」分组，不虚构 deadline** | ✅ 已确认 | **修正#2** |
| 13 | **同系列拼团聚合为一个节点，显示"等 N 件"** | ✅ 已确认 | **修正#3** |
| 14 | **直售多件显示"等 N 件"，不丢信息** | ✅ 已确认 | **修正#4** |
| 15 | **拍卖全面覆盖：竞拍中/中标待付款/囤货中/流拍退款** | ✅ 已确认 | **修正#5** |
| 16 | **Timeline 不做秒级倒计时，静态时间节点 + 即将截止标签** | ✅ 已确认 | **修正#6** |
| 17 | **节点点击先弹出详情浮层，再点「去XX」才跳转** | ✅ 已确认 | **修正#7** |
| 18 | **同时间按紧急度二级排序，红色节点视觉加粗** | ✅ 已确认 | **修正#8** |
| 19 | **拼团节点副标题显示成配进度，砍单变红** | ✅ 已确认 | **修正#9** |
| 20 | **工作台分组标题自带全选 checkbox，正常囤货默认折叠，无全局全选** | ✅ 已确认 | **修正#10** |
| 21 | **顶部常驻横向栏展示"我参与中的拼团"，同时"进行中"过滤也可见** | ✅ 已确认 | **修正#11** |

---

## 十五、文件清单（新增/改造）

| 文件 | 动作 | 说明 |
|:---|:---|:---|
| `apps/web/src/App.vue` | 改造 | 底部导航栏 + 首页路由 |
| `apps/web/src/components/Timeline.vue` | 新增 | Timeline 首页主组件（含常驻进行中拼团栏） |
| `apps/web/src/components/TimelineNode.vue` | 新增 | 单节点卡片组件 |
| `apps/web/src/components/NodeDetailSheet.vue` | 新增 | 修正#7：节点详情底部浮层 |
| `apps/web/src/components/StockWorkspace.vue` | 新增 | 批量清货工作台（含修正#10分组全选） |
| `apps/web/src/components/StatCard.vue` | 新增 | 顶部数据卡片 |
| `apps/web/src/components/ActiveGroupBar.vue` | 新增 | 修正#11：进行中拼团横向滚动栏 |
| `apps/web/src/components/ItemCard.vue` | 新增 | 通用左图右文卡片（子页面复用） |
| `apps/web/src/style.css` | 改造 | 新增 CSS 变量 + 动效 + 浮层样式 |
| `apps/web/src/utils/timeline.ts` | 新增 | 前端聚合逻辑 buildTimeline() |

---

## 十六、验收标准

### 功能验收

- [ ] Timeline 正确聚合所有节点，含拍卖 4 种状态
- [ ] 节点按时间升序 + 紧急度降序二级排序
- [ ] 顶部常驻进行中拼团栏显示所有参与中拼团
- [ ] 手动截团拼团放「截团待定」分组，不虚构时间
- [ ] 拼团同系列聚合为一个节点，显示"等 N 件"
- [ ] 直售多件显示"等 N 件"
- [ ] 拼团节点显示成配进度，砍单变红
- [ ] 节点点击弹出详情浮层，不直接跳转
- [ ] 浮层展示完整信息 + 时间线 + 「去XX」按钮
- [ ] 工作台分组全选 checkbox 正常工作
- [ ] 正常囤货分组默认折叠
- [ ] 工作台底部按钮自适应数量和总价
- [ ] 发起清货后返回 Timeline 数据刷新

### 视觉验收

- [ ] 所有卡片符合左图右文规范
- [ ] 状态色使用正确（橙/蓝/绿/红/紫）
- [ ] 超期节点左侧色条 6px + 泛红背景
- [ ] 按钮三种形态正确应用
- [ ] 无阴影，用分隔线区分卡片
- [ ] 字号层级清晰，无混用
- [ ] 移动端底部安全区域适配

### 性能验收

- [ ] Timeline 节点 100+ 时滚动流畅
- [ ] 下拉刷新响应及时
- [ ] 页面切换动画不卡顿

---

> 方案版本：v2.0
> 最后更新：2026-08-23
> 状态：已整合11项修正，待确认后执行
