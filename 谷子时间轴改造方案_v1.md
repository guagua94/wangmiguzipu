# 汪咪的谷子铺 — 团员端「时间轴 + 批量工作台」改造方案 v1.0

> 2026-08-23 | 基于方案五（时间轴信息架构）+ 方案四（谷圈原生视觉风格）融合
> 文件位置：F:/谷子APP - 副本/wangmi/谷子时间轴改造方案_v1.md

---

## 一、核心原则

1. **首页回答"我接下来要做什么"** — Timeline 按时间驱动，纯提醒，不放操作按钮
2. **工作台回答"这件事怎么批量处理"** — 自动勾选 + 自适应选中数量，不弹确认浮窗
3. **子页面回答"这个东西现在什么情况"** — 统一卡片规范，支持左图右文扫描
4. **视觉风格：谷圈原生现代风** — 保留品牌粉 + 语义状态色，卡片白底分隔线，胶囊按钮

---

## 二、首页 Timeline 架构

### 2.1 页面结构

```
┌────────────────────────────────────────────┐
│  汪咪的谷子时间轴                            │ ← 页面标题 22px/700/左对齐
│  2026-08-23 周日                             │ ← 日期辅助 13px/#8E8E93
├────────────────────────────────────────────┤
│                                              │
│  ┌─────┐  ┌─────┐  ┌─────┐                  │ ← 顶部数据卡片（横向滚动）
│  │  5  │  │  2  │  │  1  │                  │
│  │待清货│  │待付款│  │转单待│                  │
│  └─────┘  └─────┘  │确认  │                  │
│                     └─────┘                  │
│                                              │
│  ──── 接下来 ────                            │ ← 时间分组标签 13px/500/#8E8E93
│                                              │
│  今天 · 17:00                                │ ← 时间节点 12px/400/#C4C4C6
│  ┌───────────────────────────────────────┐  │ ← 节点卡片
│  │ 🟠 夏日祭系列拼团                       │  │
│  │    枫叶吧唧 #9 · 截团待付款              │  │
│  │              去清货 →                    │  │ ← "去清货" 文字链 #FF5B7F
│  └───────────────────────────────────────┘  │
│                                              │
│  3天后 · 8月26日                            │
│  ┌───────────────────────────────────────┐  │
│  │ 🟣 海盐吧唧 58mm                        │  │
│  │    该系列即将截团                        │  │
│  │              查看排表 →                 │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  12天后 · 9月4日                            │
│  ┌───────────────────────────────────────┐  │
│  │ 🔴 测试拍卖                             │  │
│  │    ⚠ 强制清货截止                       │  │
│  │    仓费已产生 ¥12.50                    │  │
│  │              去清货 →                  │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ──── 稍后 ────                             │ ← 可折叠
│  （进行中 · 3 单）                          │
│  ▼                                           │
│                                              │
│  ──── 已完成 ────                           │ ← 默认折叠
│  （已完成 · 8 单）                          │
│  ▶                                           │
│                                              │
├────────────────────────────────────────────┤
│  [🏠] [🛒] [🎲] [📋] [👤]                  │ ← 底部导航栏
└────────────────────────────────────────────┘
```

### 2.2 节点卡片规范

**单节点数据结构**：

```typescript
interface TimelineNode {
  id: string;              // 全局唯一标识，如 "bill-123" / "order-456" / "auction-789"
  type: 'group' | 'sale' | 'auction' | 'transfer' | 'clearing' | 'aftersale';
  icon: string;            // emoji 或简笔画图标：🟠🔵🟢🔴🟣
  title: string;           // 主标题，如 "夏日祭系列拼团"
  subtitle: string;        // 副标题，如 "枫叶吧唧 #9 · 截团待付款"
  deadlineAt: number;      // 关键事件截止时间戳
  statusColor: string;     // 状态色值：#FF9500 待操作 / #007AFF 进行中 / #34C759 完成 / #FF3B30 超期 / #5856D6 截止
  nextEventType: string;   // 事件类型文本："截团待付款" / "强制清货截止" / "付款截止"
  linkAction: string;      // 跳转动作："stock" / "orders" / "auction" / "transfer"
  autoSelectId?: number;   // 跳转工作台时自动勾选的 ID（如 orderId）
}
```

**节点卡片视觉规范**：

```css
.timeline-node-card {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 12px 16px;
  margin-bottom: 8px;
  border-bottom: 1px solid #F0F0F2;   /* 用分隔线，不用阴影 */
  position: relative;
  overflow: hidden;
}

/* 左侧状态色条 */
.timeline-node-card::before {
  content: '';
  position: absolute;
  left: 0;
  top: 0;
  bottom: 0;
  width: 4px;
  background: var(--status-color);
  border-radius: 12px 0 0 12px;
}

.timeline-node-title {
  font-size: 16px;
  font-weight: 600;
  color: #1C1C1E;
  padding-left: 12px;    /* 为色条留空间 */
}

.timeline-node-subtitle {
  font-size: 14px;
  font-weight: 400;
  color: #8E8E93;
  padding-left: 12px;
  margin-top: 4px;
}

.timeline-node-action {
  font-size: 14px;
  font-weight: 500;
  color: #FF5B7F;
  padding-left: 12px;
  margin-top: 8px;
  text-align: right;
}
```

**卡片点击态**：

```css
.timeline-node-card:active {
  background: #FFF5F7;
  transform: scale(0.98);
  transition: all 0.1s ease;
}
```

### 2.3 顶部数据卡片

```css
.stat-card {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 12px 16px;
  min-width: 80px;
  text-align: center;
  border: 1px solid #F0F0F2;
}

.stat-card .number {
  font-size: 32px;
  font-weight: 700;
  color: #FF5B7F;
  line-height: 1;
}

.stat-card .label {
  font-size: 12px;
  font-weight: 400;
  color: #8E8E93;
  margin-top: 4px;
}
```

**点击行为**：
- 点击后 Timeline 自动过滤为只显示对应类型的节点
- 过滤状态高亮该卡片，其他卡片置灰
- 再次点击同一卡片取消过滤，恢复全部

### 2.4 时间分组

**分组规则**：

| 分组标签 | 时间范围 | 样式 |
|:---|:---|:---|
| 今天 | deadlineAt 在 24h 内 | 正常显示 |
| 明天 | 24h-48h | 正常显示 |
| X天后 | 2-30 天 | 正常显示 |
| 本月 | 30天内 | 灰色折叠 |
| 稍后 | >30天 或 无 deadline | 灰色折叠 |
| 已完成 | 状态为已完成 | 默认折叠 |

**分组标签样式**：

```css
.time-group-label {
  font-size: 13px;
  font-weight: 500;
  color: #8E8E93;
  padding: 16px 0 8px 0;
  border-top: 1px solid #F0F0F2;
  margin-top: 8px;
}
```

### 2.5 底部过滤栏

```
[全部] [待我操作](默认选中) [进行中] [已完成 ▼]
```

**样式**：

```css
.filter-chip {
  padding: 6px 14px;
  border-radius: 999px;    /* 胶囊形 */
  font-size: 13px;
  font-weight: 500;
  background: #F2F2F7;
  color: #8E8E93;
  border: none;
  cursor: pointer;
  transition: all 0.15s;
}

.filter-chip.active {
  background: #FF5B7F;
  color: #FFFFFF;
}
```

**过滤逻辑**：
- **全部**：显示所有节点（除已完成默认折叠）
- **待我操作**：只显示需要用户主动操作的节点
  - 拼团：待付款、待收货
  - 直售：待付款、囤货中超期
  - 拍卖：待付款、中标后未入囤
  - 转单：待确认、待付款
  - 清货：待付款、已发货待确认
- **进行中**：显示状态为"进行中/囤货中/已排"的节点
- **已完成**：展开显示已完成节点

---

## 三、批量清货工作台

### 3.1 入口

**唯一入口**：
1. 点击 Timeline 节点上的 **"去清货"** 文字链
2. 点击顶部 **"待清货 X 单"** 数据卡片

**两种场景**：
- 从 Timeline 节点进入：自动勾选该单（`autoSelectId` 对应的 order）
- 从顶部卡片进入：无自动勾选，用户自己选

### 3.2 页面结构

```
┌────────────────────────────────────────────┐
│  ← 返回  📦 批量清货                         │
├────────────────────────────────────────────┤
│                                              │
│  ─── 超期（请尽快处理） ───                 │
│                                              │
│  ┌───────────────────────────────────────┐  │
│  │ ☑️ 🔴 测试拍卖                         │  │
│  │    入囤45天 · 超期15天                  │  │
│  │    仓费 ¥12.50                         │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ─── 7天内到期 ───                          │
│                                              │
│  ┌───────────────────────────────────────┐  │
│  │ ☐ 🟠 夏日祭系列拼团                     │  │
│  │    免费剩余3天                         │  │
│  └───────────────────────────────────────┘  │
│                                              │
│  ─── 正常囤货 ───                           │
│                                              │
│  ┌───────────────────────────────────────┐  │
│  │ ☐ 🔵 海盐吧唧 58mm                     │  │
│  │    入囤12天 · 免费剩余18天              │  │
│  └───────────────────────────────────────┘  │
│                                              │
│                                              │
│  ─────────────────────────────────────────  │
│  已选 2 单              预估 ¥36.50         │
│  [选择邮费 ▼] [选择打包费 ▼]                │
│  [请选择地址 ▼]                             │
│  ┌───────────────────────────────────────┐  │
│  │        发起清货（¥36.50）                │  │
│  └───────────────────────────────────────┘  │
└────────────────────────────────────────────┘
```

### 3.3 囤货卡片（工作台专用）

**与 Timeline 节点卡片的区别**：
- 左侧增加 **checkbox**
- 不显示"去清货"文字链（因为已经在工作台里了）
- 显示更详细的仓费信息

```css
.stock-card {
  background: #FFFFFF;
  border-radius: 12px;
  padding: 12px 16px;
  margin-bottom: 8px;
  border-bottom: 1px solid #F0F0F2;
  display: flex;
  align-items: flex-start;
  gap: 12px;
}

.stock-card .checkbox {
  width: 20px;
  height: 20px;
  border-radius: 4px;
  border: 2px solid #E5E5EA;
  flex-shrink: 0;
  margin-top: 2px;
  cursor: pointer;
  transition: all 0.15s;
}

.stock-card .checkbox.checked {
  background: #FF5B7F;
  border-color: #FF5B7F;
}

.stock-card .content {
  flex: 1;
  min-width: 0;
}

.stock-card .title {
  font-size: 16px;
  font-weight: 600;
  color: #1C1C1E;
}

.stock-card .subtitle {
  font-size: 14px;
  color: #8E8E93;
  margin-top: 4px;
}

.stock-card .fee {
  font-size: 14px;
  font-weight: 600;
  color: #FF3B30;    /* 超期仓费用红色 */
  margin-top: 4px;
}
```

### 3.4 紧急度分组

**分组规则**：

| 分组 | 条件 | 排序 |
|:---|:---|:---|
| 超期 | `overDays > 0` | 按超期天数倒序（最久的放最前面） |
| 7天内到期 | `0 < freeDaysLeft <= 7` | 按剩余天数正序（最快的放最前面） |
| 正常囤货 | `freeDaysLeft > 7` | 按入囤时间倒序 |

**分组标题样式**：

```css
.stock-group-header {
  font-size: 13px;
  font-weight: 500;
  color: #FF3B30;    /* 超期组用红色 */
  padding: 12px 0 8px 0;
  display: flex;
  align-items: center;
  gap: 6px;
}

.stock-group-header.normal {
  color: #8E8E93;
}
```

### 3.5 底部结算条

**固定底部**：

```css
.stock-action-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  background: #FFFFFF;
  border-top: 1px solid #F0F0F2;
  padding: 12px 16px;
  padding-bottom: calc(12px + env(safe-area-inset-bottom));
  z-index: 100;
}
```

**内部布局**：

```
┌────────────────────────────────────────────┐
│  已选 2 单              预估 ¥36.50         │ ← 第一行：统计
│  ───────────────────────────────────────  │
│  [选择邮费 ▼] [选择打包费 ▼]                │ ← 第二行：选项（横向滚动）
│  [请选择地址 ▼]                             │
│  ┌───────────────────────────────────────┐│ ← 第三行：主按钮
│  │        发起清货（¥36.50）                ││
│  └───────────────────────────────────────┘│
└────────────────────────────────────────────┘
```

**按钮自适应文案**：

```javascript
const buttonText = computed(() => {
  const count = selectedOrders.value.length;
  const total = estimatedTotal.value;
  if (count === 0) return '请选择要清的囤货';
  return `发起清货（${count}单 · ¥${total.toFixed(2)}）`;
});
```

**未选时禁用**：

```css
.btn-primary:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}
```

---

## 四、子页面统一卡片规范

### 4.1 卡片基础结构

**所有子页面（订单、转单、售后、余额流水等）统一使用**：

```
┌────────────────────────────────────────────┐
│  ┌────┐  标题行（16px/600/#1C1C1E）         │
│  │ 🟠 │  副标题行（14px/400/#8E8E93）       │
│  └────┘  操作行（14px/500/#FF5B7F 右对齐）  │
└────────────────────────────────────────────┘
```

### 4.2 状态色圆点/色条规范

| 状态 | 颜色 | 用途 |
|:---|:---|:---|
| 待付款/待操作 | `#FF9500` 橙色 | 需要用户主动行动 |
| 进行中/囤货中/已排 | `#007AFF` 蓝色 | 正常进行 |
| 已完成/成功 | `#34C759` 绿色 | 流程结束 |
| 超期/警告/失败 | `#FF3B30` 红色 | 需要紧急处理 |
| 截团/截止 | `#5856D6` 紫色 | 时间类事件 |

**色条宽度**：Timeline 节点用 4px 左侧色条；工作台卡片用 checkbox + 无侧色条（用标签色区分）

### 4.3 标签(Tag)规范

```css
.tag {
  display: inline-block;
  padding: 2px 8px;
  border-radius: 4px;
  font-size: 11px;
  font-weight: 500;
  line-height: 1.4;
}

.tag-pink {
  background: #FFE4EA;
  color: #FF5B7F;
}

.tag-blue {
  background: #E8F4FF;
  color: #007AFF;
}

.tag-green {
  background: #E8F8ED;
  color: #34C759;
}

.tag-orange {
  background: #FFF5E6;
  color: #FF9500;
}

.tag-red {
  background: #FFEBEE;
  color: #FF3B30;
}

.tag-gray {
  background: #F2F2F7;
  color: #8E8E93;
}
```

### 4.4 按钮规范

**三类按钮**：

```css
/* 主按钮 */
.btn-primary {
  background: #FF5B7F;
  color: #FFFFFF;
  border: none;
  border-radius: 999px;    /* 胶囊形 */
  padding: 12px 24px;
  font-size: 15px;
  font-weight: 600;
  min-height: 48px;
  width: 100%;
  cursor: pointer;
  transition: all 0.15s;
}

.btn-primary:hover {
  background: #E0486B;
}

.btn-primary:active {
  transform: scale(0.97);
}

/* 次按钮 */
.btn-secondary {
  background: #FFFFFF;
  color: #FF5B7F;
  border: 1.5px solid #FF5B7F;
  border-radius: 8px;
  padding: 10px 20px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  transition: all 0.15s;
}

.btn-secondary:hover {
  background: #FFF5F7;
}

/* 文字链 */
.link-action {
  color: #FF5B7F;
  font-size: 14px;
  font-weight: 500;
  text-decoration: none;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  gap: 4px;
}

.link-action::after {
  content: '→';
  font-size: 16px;
}
```

### 4.5 各子页面卡片示例

#### 订单页

```
┌────────────────────────────────────────────┐
│  🟠 夏日祭系列拼团                           │
│     枫叶吧唧×2 海盐吧唧×1                  │
│     待付款 · ¥128.00                        │
│                        去付款 →             │
└────────────────────────────────────────────┘
```

#### 转单页

```
┌────────────────────────────────────────────┐
│  🟠 枫叶吧唧 #9                             │
│     → 转给 小明                             │
│     ⏳ 剩余 12:34:56                         │
│                        撤销 →               │
└────────────────────────────────────────────┘
```

#### 肾表页

```
┌────────────────────────────────────────────┐
│  📊 夏日祭系列拼团 肾表                      │
│     截团：8/25 · 已排3件                     │
│                        查看排表 →            │
├────────────────────────────────────────────┤
│  谷子名称        已排/总量    我的           │
│  ──────────────────────────────────────     │
│  🟢 枫叶吧唧      45/50       #3 #7         │
│  🔴 海盐吧唧      52/50       #9(候补)      │
│  🟡 花火立牌      30/30       -             │
└────────────────────────────────────────────┘
```

---

## 五、视觉设计规范

### 5.1 配色体系

```css
:root {
  /* 品牌色 */
  --brand: #FF5B7F;
  --brand-dark: #E0486B;
  --brand-bg: #FFE4EA;
  --brand-bg2: #FFF5F7;

  /* 语义状态色 */
  --status-pending: #FF9500;    /* 待付款/待操作 */
  --status-active: #007AFF;     /* 进行中/囤货中 */
  --status-done: #34C759;       /* 已完成 */
  --status-warning: #FF3B30;    /* 超期/警告 */
  --status-deadline: #5856D6;   /* 截团/截止 */

  /* 文字层级 */
  --t1: #1C1C1E;    /* 主标题 */
  --t2: #3A3A3C;    /* 正文 */
  --t3: #8E8E93;    /* 辅助 */
  --t4: #C4C4C6;    /* 置灰 */

  /* 背景/边框 */
  --bg: #FAFAFA;
  --card: #FFFFFF;
  --line: #F0F0F2;
  --line2: #E5E5EA;
}
```

### 5.2 字号体系

| 层级 | 字号 | 字重 | 用途 | 色值 |
|:---|:---|:---|:---|:---|
| 页面大标题 | 22px | 700 | "汪咪的谷子时间轴" | --t1 |
| 数据卡片数字 | 32px | 700 | 仪表盘"5" | --brand |
| 卡片标题 | 16px | 600 | 系列名、商品名 | --t1 |
| 正文 | 14px | 400 | 常规内容 | --t2 |
| 辅助文字 | 12px | 400 | 日期、状态、金额说明 | --t3 |
| 时间分组标签 | 13px | 500 | "今天 · 17:00" | --t3 |
| 标签文字 | 11px | 500 | 拼团/直售/拍卖标签 | 白色 |

### 5.3 间距体系

| 元素 | 上 | 右 | 下 | 左 |
|:---|:---|:---|:---|:---|
| 页面边距 | - | 16px | - | 16px |
| 卡片内边距 | 12px | 16px | 12px | 16px |
| 卡片间距 | - | - | 8px | - |
| 按钮内边距 | 12px | 24px | 12px | 24px |
| 表单输入框 | 10px | 12px | 10px | 12px |

### 5.4 圆角体系

| 元素 | 圆角 |
|:---|:---|
| 页面卡片 | 12px |
| 按钮 | 999px（胶囊） |
| 次按钮/输入框 | 8px |
| 过滤Chip | 999px |
| 标签 | 4px |

### 5.5 阴影规范

**全局不使用阴影**，用以下替代：
- 卡片间：8px 间距 + 无分隔线（靠留白区分）
- 卡片内：1px `#F0F0F2` 底部分隔线
- 底部固定条：1px `#F0F0F2` 顶部分隔线

---

## 六、动效与交互规范

### 6.1 页面过渡

```css
/* 从右侧滑入 */
.page-enter {
  animation: slideIn 0.25s ease-out;
}

@keyframes slideIn {
  from {
    opacity: 0;
    transform: translateX(30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}
```

### 6.2 卡片点击态

```css
.card {
  transition: all 0.1s ease;
}

.card:active {
  background: #FFF5F7;
  transform: scale(0.98);
}
```

### 6.3 Timeline 节点展开

```css
.time-group-content {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease-out;
}

.time-group-content.expanded {
  max-height: 1000px;    /* 足够大的值 */
}
```

### 6.4 数字滚动动画

顶部数据卡片的数字变化时，使用 CountUp 效果：

```javascript
// 从旧值滚动到新值，持续 0.5s
function animateNumber(el, from, to, duration = 500) {
  const start = performance.now();
  function tick(now) {
    const progress = Math.min((now - start) / duration, 1);
    const value = Math.round(from + (to - from) * progress);
    el.textContent = value;
    if (progress < 1) requestAnimationFrame(tick);
  }
  requestAnimationFrame(tick);
}
```

### 6.5 下拉刷新

```css
.pull-refresh {
  position: relative;
  overflow: hidden;
}

.pull-refresh-indicator {
  position: absolute;
  top: -50px;
  left: 0;
  right: 0;
  height: 50px;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 0.2s;
}

.pull-refresh-indicator.pulling {
  transform: translateY(50px);
}
```

### 6.6 Toast 提示

```css
.toast {
  position: fixed;
  top: 16px;
  left: 50%;
  transform: translateX(-50%) translateY(-100px);
  background: #1C1C1E;
  color: #FFFFFF;
  padding: 10px 20px;
  border-radius: 8px;
  font-size: 14px;
  z-index: 1000;
  opacity: 0;
  transition: all 0.3s ease;
}

.toast.show {
  transform: translateX(-50%) translateY(0);
  opacity: 1;
}

/* 成功态 */
.toast.success {
  background: #34C759;
}

/* 错误态 */
.toast.error {
  background: #FF3B30;
}
```

---

## 七、数据聚合与接口建议

### 7.1 新增接口：GET /timeline/mine

**后端聚合 Timeline 数据，按 `nextEventAt` 排序后返回。**

**请求**：

```
GET /timeline/mine
```

**响应**：

```json
{
  "stats": {
    "pendingClearing": 5,      // 待清货单数
    "pendingPayment": 2,       // 待付款单数
    "pendingTransfer": 1,     // 转单待确认数
    "pendingReceive": 0       // 待收货单数
  },
  "nodes": [
    {
      "id": "bill-123",
      "type": "group",
      "icon": "🟠",
      "title": "夏日祭系列拼团",
      "subtitle": "枫叶吧唧 #9 · 截团待付款",
      "deadlineAt": 1724419200000,
      "statusColor": "#FF9500",
      "nextEventType": "截团待付款",
      "linkAction": "stock",
      "autoSelectId": 123
    },
    {
      "id": "auction-456",
      "type": "auction",
      "icon": "🔴",
      "title": "测试拍卖",
      "subtitle": "⚠ 强制清货截止 · 仓费已产生 ¥12.50",
      "deadlineAt": 1725273600000,
      "statusColor": "#FF3B30",
      "nextEventType": "强制清货截止",
      "linkAction": "stock",
      "autoSelectId": 456
    }
  ]
}
```

**聚合逻辑（后端建议）**：

```typescript
// 伪代码
async function getTimeline(uid: number) {
  const nodes: TimelineNode[] = [];

  // 1. 拼团：已排但未付款的谷序
  const bills = await getMyBills(uid);
  for (const bill of bills.filter(b => b.state === '待付款')) {
    for (const item of bill.items) {
      for (const seq of item.seqs.split(',')) {
        nodes.push({
          id: `bill-${bill.id}-${item.goodId}-${seq}`,
          type: 'group',
          title: bill.seriesName,
          subtitle: `${item.name} #${seq} · 截团待付款`,
          deadlineAt: new Date(bill.createdAt).getTime() + 48 * 3600 * 1000, // 截团后48h付款
          statusColor: '#FF9500',
          nextEventType: '截团待付款',
          linkAction: 'orders',
        });
      }
    }
  }

  // 2. 直售：待付款订单
  const buys = await getMyBuys(uid);
  for (const buy of buys.filter(o => o.status === '待付款')) {
    nodes.push({
      id: `order-${buy.id}`,
      type: 'sale',
      title: buy.items.map(i => i.name).join('、'),
      subtitle: `待付款 · ¥${buy.total}`,
      deadlineAt: new Date(buy.createdAt).getTime() + 24 * 3600 * 1000, // 24h付款
      statusColor: '#FF9500',
      nextEventType: '付款截止',
      linkAction: 'orders',
      autoSelectId: buy.id,
    });
  }

  // 3. 拍卖：待付款中标
  const auctions = await getMyAuctions(uid);
  for (const auction of auctions.filter(a => a.state === '待付款' && a.isWinner)) {
    nodes.push({
      id: `auction-${auction.id}`,
      type: 'auction',
      title: auction.name,
      subtitle: `中标待付款 · ¥${auction.curPrice}`,
      deadlineAt: (auction.wonAt || Date.now()) + 24 * 3600 * 1000,
      statusColor: '#FF9500',
      nextEventType: '付款截止',
      linkAction: 'auction',
      autoSelectId: auction.id,
    });
  }

  // 4. 囤货中（拼团/直售/拍卖）
  const stockOrders = [...groupStocks, ...saleStocks, ...auctionStocks];
  for (const order of stockOrders) {
    const days = getStockDays(order);
    const freeDays = order.seriesId ? 30 : 7;
    const overDays = Math.max(0, days - freeDays);
    const freeLeft = Math.max(0, freeDays - days);

    let subtitle: string;
    let statusColor: string;
    let nextEventType: string;

    if (overDays > 0) {
      subtitle = `入囤${days}天 · 超期${overDays}天 · 仓费¥${calcOverFee(order).toFixed(2)}`;
      statusColor = '#FF3B30';
      nextEventType = '超期清货';
    } else if (freeLeft <= 7) {
      subtitle = `入囤${days}天 · 免费剩余${freeLeft}天 ⚠`;
      statusColor = '#FF9500';
      nextEventType = '即将到期';
    } else {
      subtitle = `入囤${days}天 · 免费剩余${freeLeft}天`;
      statusColor = '#007AFF';
      nextEventType = '正常囤货';
    }

    nodes.push({
      id: `stock-${order.id}`,
      type: order.isAuction ? 'auction' : (order.seriesId ? 'group' : 'sale'),
      title: order.seriesName || order.items[0]?.name || '未命名',
      subtitle,
      deadlineAt: order.paidAt ? new Date(order.paidAt).getTime() + freeDays * 24 * 3600 * 1000 : Date.now(),
      statusColor,
      nextEventType,
      linkAction: 'stock',
      autoSelectId: order.id,
    });
  }

  // 5. 转单
  const transfers = await getMyTransfers(uid);
  for (const t of transfers.filter(t => ['待接收者确认', '待管理员审核', '待接收者付款'].includes(t.state))) {
    nodes.push({
      id: `transfer-${t.id}`,
      type: 'transfer',
      title: t.name,
      subtitle: `${t.fromCn} → ${t.toCn} · ${t.state}`,
      deadlineAt: t.deadline,
      statusColor: '#FF9500',
      nextEventType: t.state,
      linkAction: 'transfer',
    });
  }

  // 6. 清货单（待付款/已发货）
  const clears = await getMyClearings(uid);
  for (const c of clears.filter(c => ['待付款', '已发货'].includes(c.state))) {
    nodes.push({
      id: `clearing-${c.id}`,
      type: 'clearing',
      title: `清货单 #${c.id}`,
      subtitle: `${c.state} · ¥${c.total}`,
      deadlineAt: c.state === '待付款' ? Date.now() + 24 * 3600 * 1000 : Date.now() + 7 * 24 * 3600 * 1000,
      statusColor: c.state === '待付款' ? '#FF9500' : '#007AFF',
      nextEventType: c.state,
      linkAction: 'stock',
    });
  }

  // 按 deadlineAt 升序排序（最近的在最上面）
  nodes.sort((a, b) => a.deadlineAt - b.deadlineAt);

  return {
    stats: {
      pendingClearing: stockOrders.filter(o => overDays(o) > 0 || freeDaysLeft(o) <= 7).length,
      pendingPayment: bills.length + buys.filter(o => o.status === '待付款').length + auctions.filter(a => a.state === '待付款').length,
      pendingTransfer: transfers.filter(t => t.state === '待接收者确认').length,
      pendingReceive: clears.filter(c => c.state === '已发货').length,
    },
    nodes,
  };
}
```

**注意**：如果后端聚合成本太高，也可以由前端在 `loadMe()` 时聚合已有数据（`myBills`、`myBuys`、`myAuctionOrders`、`myTransfers`、`myClears`），复用现有接口，无需新增后端接口。

### 7.2 现有接口复用

前端已有数据（`loadMe()` 加载的）：

| 数据 | 来源 |
|:---|:---|
| myBills | `GET /group/my-bills` |
| myBuys | `GET /sale/my-orders` |
| myAuctionOrders | `GET /auction/my-orders` |
| myTransfers | `GET /transfer/mine` |
| myClears | `GET /clearing/mine` |
| shopCfg | `GET /shop/config` |

**前端聚合方案**：在 `loadMe()` 后，调用一个新的 `buildTimeline()` 函数，把上述数据聚合成 TimelineNode[]。

### 7.3 批量清货工作台接口

复用现有接口，无需新增：

- `GET /sale/my-orders` — 获取囤货中订单
- `GET /shop/config` — 获取邮费/打包费配置
- `GET /address/list` — 获取收货地址
- `POST /clearing/create` — 发起清货（已有，参数不变）

---

## 八、底部导航栏改造

### 8.1 导航项

```
[🏠 首页] [🛒 拼团] [🎲 拍卖] [📋 订单] [👤 我的]
```

**首页**：Timeline 仪表盘（新增，替代原来的"我的"首页）
**拼团**：拼团大厅（保留现有）
**拍卖**：拍卖大厅（保留现有）
**订单**：订单列表（原"我的"里的订单模块独立出来）
**我的**：个人中心（设置、地址、余额、售后等次要功能）

### 8.2 样式

```css
.tab-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 56px;
  background: #FFFFFF;
  border-top: 1px solid #F0F0F2;
  display: flex;
  justify-content: space-around;
  align-items: center;
  padding-bottom: env(safe-area-inset-bottom);
  z-index: 100;
}

.tab-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  color: #8E8E93;
  font-size: 10px;
  transition: color 0.15s;
}

.tab-item.active {
  color: #FF5B7F;
}

/* 图标用 SVG，未选中线框，选中填充 */
.tab-icon {
  width: 24px;
  height: 24px;
}
```

---

## 九、改造范围与优先级

### 第一阶段（MVP，先上线Timeline首页）

| 优先级 | 任务 | 范围 | 工作量 |
|:---|:---|:---|:---|
| P0 | Timeline 首页 | 新建 `Timeline.vue` 组件，聚合已有数据，展示时间轴 | 2-3天 |
| P0 | 顶部数据卡片 | Timeline 顶部 4 个统计卡片，点击过滤 | 0.5天 |
| P0 | 底部导航改造 | 新增"首页"tab，调整图标样式 | 0.5天 |
| P1 | 批量清货工作台 | 改造现有 `meSubTab === 'stock'` 页面，按紧急度分组，自动勾选 | 1-2天 |
| P1 | 统一卡片样式 | Timeline 节点卡片、工作台卡片统一规范 | 0.5天 |

### 第二阶段（子页面规范化）

| 优先级 | 任务 | 范围 |
|:---|:---|:---|
| P2 | 订单页改造 | 取消 tab，改为 Chip 过滤 + Timeline 风格卡片 |
| P2 | 转单页改造 | 保留 tab，卡片规范化，加倒计时 |
| P2 | 肾表页改造 | 按系列折叠，每行一个谷子 |
| P2 | 余额/售后等次要页面 | 统一卡片 + 按钮规范 |

### 第三阶段（动效与细节）

| 优先级 | 任务 | 范围 |
|:---|:---|:---|
| P3 | 页面过渡动画 | 滑入 + 淡入 |
| P3 | 下拉刷新 | 回弹动画 |
| P3 | 数字滚动 | 仪表盘数字变化动画 |
| P3 | Toast 提示 | 替代 alert |

---

## 十、关键决策确认清单

| # | 决策 | 状态 |
|:---|:---|:---|
| 1 | Timeline 首页纯提醒，节点不放操作按钮 | ✅ 已确认 |
| 2 | 批量清货工作台是唯一操作入口 | ✅ 已确认 |
| 3 | Timeline 节点"去清货"跳转工作台并自动勾选 | ✅ 已确认 |
| 4 | 工作台自适应选中数量，不弹确认浮窗 | ✅ 已确认 |
| 5 | 配色保留品牌粉，增加语义状态色 | ✅ 已确认 |
| 6 | 卡片用左图右文 + 分隔线，不用阴影 | ✅ 已确认 |
| 7 | 按钮用胶囊形主按钮 + 次按钮 + 文字链 | ✅ 已确认 |
| 8 | 首页改为 Timeline，底部导航新增"首页"tab | ✅ 已确认 |
| 9 | 工作台按紧急度分组排序 | ✅ 已确认 |
| 10 | 后端聚合 vs 前端聚合（待实施时确认） | ⏳ 待定 |

---

## 十一、文件清单（新增/改造）

| 文件 | 动作 | 说明 |
|:---|:---|:---|
| `apps/web/src/App.vue` | 改造 | 底部导航栏 + 首页路由指向 Timeline |
| `apps/web/src/components/Timeline.vue` | 新增 | Timeline 首页主组件 |
| `apps/web/src/components/TimelineNode.vue` | 新增 | 单节点卡片组件 |
| `apps/web/src/components/StockWorkspace.vue` | 新增 | 批量清货工作台 |
| `apps/web/src/components/StatCard.vue` | 新增 | 顶部数据卡片 |
| `apps/web/src/components/ItemCard.vue` | 新增 | 通用左图右文卡片（子页面复用） |
| `apps/web/src/style.css` | 改造 | 新增 CSS 变量 + 转单弹窗样式（保留） |
| `apps/server/src/modules/timeline.ts` | 可选新增 | 后端聚合接口（如果选后端聚合） |

---

## 十二、验收标准

### 功能验收

- [ ] Timeline 首页正确聚合所有待办节点
- [ ] 节点按 deadlineAt 升序排列
- [ ] 顶部数据卡片统计正确
- [ ] 点击数据卡片能过滤 Timeline
- [ ] 点击"去清货"跳转工作台并自动勾选对应单
- [ ] 工作台支持多选/全选
- [ ] 工作台底部按钮自适应显示数量和总价
- [ ] 发起清货后返回 Timeline，数据刷新

### 视觉验收

- [ ] 所有卡片符合左图右文规范
- [ ] 状态色使用正确（橙/蓝/绿/红/紫）
- [ ] 按钮三种形态正确应用
- [ ] 无阴影，用分隔线区分卡片
- [ ] 字号层级清晰，无混用
- [ ] 移动端底部安全区域适配

### 性能验收

- [ ] Timeline 节点 100+ 时滚动流畅
- [ ] 下拉刷新响应及时
- [ ] 页面切换动画不卡顿

---

> 方案版本：v1.0
> 最后更新：2026-08-23
> 状态：待确认后执行
